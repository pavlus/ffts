language: c
os:
  - linux
  - osx
addons:
  apt:
    packages:
      - cmake
    sources:
      - kubuntu-backports
script:
  - mkdir build && cd build && cmake .. && cmake --build .
  - $TRAVIS_BUILD_DIR/build_android.sh
  - $TRAVIS_BUILD_DIR/build_iphone.sh
#deploy:
#  provider: releases
#  api_key: "GITHUB OAUTH TOKEN"
#  file: "FILE TO UPLOAD"
#  skip_cleanup: true
#  on:
#    tags: true
