language: c
os:
  - linux
  - osx
addons:
  apt:
    packages:
      - cmake
    sources:
      - kubuntu-backports
before-script:
  - cd $TRAVIS_BUILD_DIR
script:
  - mkdir build && cd build && cmake .. && cmake --build .
  - ./build_android.sh
  - ./build_iphone.sh
#deploy:
#  provider: releases
#  api_key: "GITHUB OAUTH TOKEN"
#  file: "FILE TO UPLOAD"
#  skip_cleanup: true
#  on:
#    tags: true
